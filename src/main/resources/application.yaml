spring:
  datasource:
    url: jdbc:h2:~/saudi-city;USER=test;PASSWORD=test;DB_CLOSE_ON_EXIT=FALSE
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update